
/*⚠️ NE MODIFIEZ PAS LE NOM DES DÉCLARATIONS ⚠️*/

/*2️⃣ EXERCICE 2️⃣*/

function returnString(string) {
    // Must return a string.
    // Your code:
    return string;
 }
 console.log(returnString('webster'));

 
 // ⛔️ "X" and "Y" are numbers.
 
 
 function sum(x, y) {
    // Return the result of their sum.
    // Your code:
    let add = x + y;
    return add;
 }
console.log(`Le resultat de l'addition est : ${(sum(4 , 5))}`)

 
function sub(x, y) {
    // Return the result of their sub.
    // Your code:
    let sub = x - y;
    return sub;
 }
console.log(`Le resultat de la soustraction est : ${(sub(4 , 5))}`)
 
function divide(x, y) {
    // Return the result of their sub.
    // Your code:
    let div = x / y;
    return div;
 }
console.log(`Le resultat de la division est : ${(divide(16 , 8))}`)
 
function multiply(x, y) {
    // Return the result of their multip .
    // Your code:
    let multip = x * y;
    return multip;
 }
console.log(`Le resultat de la multiplication est : ${(multiply(3 , 8))}`)
 
function getRest(x, y) {
    //Find the remainder of the division of "x" by "y".
    // Your code:
    let getRest = x % y;
    return getRest;
 }
 console.log(getRest(13 , 5))